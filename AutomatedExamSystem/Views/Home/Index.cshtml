@* @{
    ViewData["Title"] = "Welcome | Automated Exam System";
}

<!-- Bootstrap & Lottie -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://unpkg.com/@@lottiefiles/lottie-player@latest/dist/lottie-player.js"></script>

<style>
    body {
        font-family: 'Poppins', sans-serif;
        margin: 0;
        overflow: hidden;
        transition: background 0.6s, color 0.6s;
    }

        /* 🌞 Light mode */
        body.light-mode {
            background: linear-gradient(135deg, #e3f2fd, #ffffff);
            color: #001f3f;
        }

        /* 🌙 Dark mode */
        body.dark-mode {
            background: radial-gradient(circle at 30% 30%, #0d1b2a, #000814);
            color: #e0e0e0;
        }

    .hero {
        height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        position: relative;
        z-index: 2;
        margin-bottom:2rem;
    }

        .hero h1 {
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .hero a {
            
            margin-bottom: 10rem;
        }

        .hero p {
            font-size: 1.2rem;
            opacity: 0.9;
        }

    .btn-custom {
        background-color: #004aad;
        border: none;
        border-radius: 50px;
        color: #fff;
        padding: 12px 35px;
        font-weight: 600;
        margin-top: 25px;
        transition: 0.3s;
    }

        .btn-custom:hover {
            background-color: #002b7f;
            transform: translateY(-2px);
        }

    /* Dark mode button */
    body.dark-mode .btn-custom {
        background-color: #00b4d8;
    }

        body.dark-mode .btn-custom:hover {
            background-color: #0077b6;
        }

    /* Toggle Switch */
    .toggle-container {
        position: fixed;
        top: 20px;
        right: 25px;
        display: flex;
        align-items: center;
        z-index: 10;
    }

    .toggle-label {
        margin-right: 10px;
        font-weight: 600;
    }

    .toggle-switch {
        position: relative;
        width: 55px;
        height: 28px;
        background: #ccc;
        border-radius: 50px;
        cursor: pointer;
        transition: background 0.3s;
    }

        .toggle-switch::after {
            content: "";
            position: absolute;
            top: 3px;
            left: 3px;
            width: 22px;
            height: 22px;
            border-radius: 50%;
            background: white;
            transition: 0.3s;
        }

    body.dark-mode .toggle-switch {
        background: #0077b6;
    }

        body.dark-mode .toggle-switch::after {
            transform: translateX(27px);
        }

    /* Animated background waves */
    .bg-waves {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        overflow: hidden;
        z-index: 0;
    }

    .wave {
        position: absolute;
        width: 200%;
        height: 100%;
        background: rgba(0, 74, 173, 0.2);
        border-radius: 43%;
        animation: waveMove 10s linear infinite;
    }

        .wave:nth-child(2) {
            animation-delay: -5s;
            opacity: 0.4;
        }

    @@keyframes waveMove {
        from

    {
        transform: rotate(0deg);
    }

    to {
        transform: rotate(360deg);
    }

    }

    body.dark-mode .wave {
        background: rgba(0, 180, 216, 0.2);
    }
</style>

<!-- 🌗 Dark Mode Toggle -->
<div class="toggle-container">
    <span class="toggle-label">🌙</span>
    <div id="themeToggle" class="toggle-switch"></div>
</div>

<!-- 🌊 Background Waves -->
<div class="bg-waves">
    <div class="wave"></div>
    <div class="wave"></div>
</div>

<!-- ✨ Hero Section -->
<div class="hero">
    <lottie-player src="https://assets7.lottiefiles.com/packages/lf20_yr6zz3wv.json"
                   background="transparent"
                   speed="1"
                   style="width: 280px; height: 280px;"
                   loop
                   autoplay>
    </lottie-player>

    <h1>Welcome to the Perfetti Van Melle Undergraduate Scholarship Assessment. </h1>
    <i>Powered by Fort Knox Outsourcing</i>
    <p>This platform is your gateway to excellence.<b><strong>Perfetti Van Melle</strong>  seeks to recognize students</b> who demonstrate analytical ability, creativity, and<b> a growth mindset — the qualities</b> that drive success in today’s dynamic world..</p>
    
        <h4>Take a deep breath, stay focused, and give your best.</h4>
    <p>
        Your journey toward opportunity and excellence starts here.
    </p>

    <a href="@Url.Action("Login", "Account")" class="btn btn-custom">Start Exam</a>
</div>

<script>
    const toggle = document.getElementById('themeToggle');
    const currentTheme = localStorage.getItem('theme');

    // Apply saved theme on load
    if (currentTheme === 'dark') {
        document.body.classList.add('dark-mode');
    } else {
        document.body.classList.add('light-mode');
    }

    // Toggle on click
    toggle.addEventListener('click', () => {
        document.body.classList.toggle('dark-mode');
        document.body.classList.toggle('light-mode');
        const theme = document.body.classList.contains('dark-mode') ? 'dark' : 'light';
        localStorage.setItem('theme', theme);
    });
</script>
 *@











@{
    ViewData["Title"] = "Welcome | Automated Exam System";
}

<!-- Bootstrap & Lottie -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="https://unpkg.com/@@lottiefiles/lottie-player@latest/dist/lottie-player.js"></script>

<style>
    body {
        font-family: 'Poppins', sans-serif;
        margin: 0;
        transition: background 0.6s, color 0.6s;
        overflow-x: hidden;
    }

        body.light-mode {
            background: linear-gradient(135deg, #e3f2fd, #ffffff);
            color: #002b5c;
        }

        body.dark-mode {
            background: radial-gradient(circle at 30% 30%, #001a33, #000814);
            color: #e0e0e0;
        }

    .hero {
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        position: relative;
        z-index: 2;
        padding: 2rem;
    }

    .hero-card {
        background: rgba(255, 255, 255, 0.9);
        border-radius: 25px;
        box-shadow: 0 10px 25px rgba(0, 74, 173, 0.2);
        padding: 3rem;
        max-width: 900px;
        backdrop-filter: blur(10px);
        transition: transform 0.4s ease, box-shadow 0.4s ease;
    }

        .hero-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(0, 74, 173, 0.25);
        }

    body.dark-mode .hero-card {
        background: rgba(0, 0, 0, 0.6);
        box-shadow: 0 10px 25px rgba(0, 180, 216, 0.3);
    }

    h1 {
        font-size: 2.4rem;
        font-weight: 700;
        color: #004aad;
        margin-bottom: 0.5rem;
    }

    body.dark-mode h1 {
        color: #00b4d8;
    }

    .hero p {
        font-size: 1.1rem;
        margin: 1rem auto;
        max-width: 700px;
        opacity: 0.9;
        line-height: 1.6;
    }

    .btn-custom {
        background-color: #e63946;
        border: none;
        border-radius: 50px;
        color: #fff;
        padding: 12px 35px;
        font-weight: 600;
        margin-top: 25px;
        transition: all 0.3s ease-in-out;
        box-shadow: 0 5px 15px rgba(230, 57, 70, 0.3);
    }

        .btn-custom:hover {
            background-color: #c91c28;
            transform: translateY(-3px);
            box-shadow: 0 8px 18px rgba(230, 57, 70, 0.4);
        }

    /* Toggle Switch */
    .toggle-container {
        position: fixed;
        top: 20px;
        right: 25px;
        display: flex;
        align-items: center;
        z-index: 10;
    }

    .toggle-label {
        margin-right: 10px;
        font-weight: 600;
    }

    .toggle-switch {
        position: relative;
        width: 55px;
        height: 28px;
        background: #ccc;
        border-radius: 50px;
        cursor: pointer;
        transition: background 0.3s;
    }

        .toggle-switch::after {
            content: "";
            position: absolute;
            top: 3px;
            left: 3px;
            width: 22px;
            height: 22px;
            border-radius: 50%;
            background: white;
            transition: 0.3s;
        }

    body.dark-mode .toggle-switch {
        background: #0077b6;
    }

        body.dark-mode .toggle-switch::after {
            transform: translateX(27px);
        }

    /* Animated background waves */
    .bg-waves {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        overflow: hidden;
        z-index: 0;
    }

    .wave {
        position: absolute;
        width: 200%;
        height: 100%;
        background: rgba(0, 74, 173, 0.2);
        border-radius: 43%;
        animation: waveMove 12s linear infinite;
    }

        .wave:nth-child(2) {
            animation-delay: -6s;
            opacity: 0.4;
        }

    @@keyframes waveMove {
        from

    {
        transform: rotate(0deg);
    }

    to {
        transform: rotate(360deg);
    }

    }

    body.dark-mode .wave {
        background: rgba(0, 180, 216, 0.2);
    }

    .fade-in {
        opacity: 0;
        transform: translateY(20px);
        animation: fadeInUp 1s ease forwards;
    }

    @@keyframes fadeInUp {
        to

    {
        opacity: 1;
        transform: translateY(0);
    }

    }

    /* ✅ Fort Knox Logo Placement */
    .footer-logo {
        position: fixed;
        bottom: 15px;
        right: 20px;
        text-align: right;
        z-index: 5;
        animation: fadeInUp 1s ease forwards;
    }

        .footer-logo img {
            width: 140px;
            height: auto;
            display: block;
            margin-left: auto;
        }

        .footer-logo p {
            font-style: italic;
            color: #004aad;
            font-size: 0.9rem;
            margin-top: 4px;
        }

    body.dark-mode .footer-logo p {
        color: #00b4d8;
    }
</style>

<!-- 🌗 Dark Mode Toggle -->
<div class="toggle-container">
    <span class="toggle-label">🌙</span>
    <div id="themeToggle" class="toggle-switch"></div>
</div>

<!-- 🌊 Background Waves -->
<div class="bg-waves">
    <div class="wave"></div>
    <div class="wave"></div>
</div>

<!-- ✨ Hero Section -->
<section class="hero">
    <div class="hero-card fade-in">
        <img src="~/images/exam.jpeg" alt="index logo" />

        <h1>Welcome to the Perfetti Van Melle Undergraduate Scholarship Assessment</h1>
        <i>Powered by Fort Knox Outsourcing</i>

        <p>
            This platform is your gateway to excellence.
            <b><strong>Perfetti Van Melle</strong> seeks to recognize students</b> who demonstrate analytical ability, creativity,
            and a <b>growth mindset</b> — the qualities that drive success in today’s dynamic world.
        </p>

        <h4>Take a deep breath, stay focused, and give your best.</h4>
        <p>Your journey toward opportunity and excellence starts here.</p>

       @*  <a href="@Url.Action("Login", "Account")" class="btn btn-custom">Start Exam</a> *@
        <a href="@Url.Action("Instructions", "Home")" class="btn btn-custom">Start Exam</a>

    </div>
</section>

<!-- ✅ Fort Knox Logo with Label -->
<!-- 🌐 Fort Knox Outsourcing Logo -->
<div class="position-fixed bottom-0 end-0 mb-3 me-3 text-center">
    <a href="https://www.fortknoxgroup.com/" target="_blank" rel="noopener noreferrer">
        <img src="~/images/fortknox_logo.jpg" alt="Fort Knox Outsourcing Logo"
             style="width: 160px; height: auto; opacity: 0.9; transition: opacity 0.3s;">
    </a>
    <div class="small fst-italic text-muted">Technology and Talent Partner</div>

</div>

@* <div class="footer-logo">
    <img src="~/images/fortknox_logo.jpg" alt="Fort Knox Outsourcing Logo" />
    <p>Technology and Talent Partner.</p>
</div> 
 *@
<!-- 🌙 Theme Toggle Script -->
<script>
    const toggle = document.getElementById('themeToggle');
    const currentTheme = localStorage.getItem('theme');

    if (currentTheme === 'dark') {
        document.body.classList.add('dark-mode');
    } else {
        document.body.classList.add('light-mode');
    }

    toggle.addEventListener('click', () => {
        document.body.classList.toggle('dark-mode');
        document.body.classList.toggle('light-mode');
        const theme = document.body.classList.contains('dark-mode') ? 'dark' : 'light';
        localStorage.setItem('theme', theme);
    });
</script>
